import "ya" Ya
import "ya-world" Ya.World
import "ya-ascii" Ya.ASCII
import "ya-tutorials" Ya.Expo.Instances ()
import "ya-console" Ya.Console

import "base" GHC.Num (Integer)
import "base" System.IO (print)

-- main = print @(Unit `P` Unit) (Unit `hj` is `hv` Unit)

-- (i `AR` a) `P` i -> into (from a o) o
-- main = print @Unit (is `lu` Unit `hd_` is)

parse = on @Glyph `ho'ho` on @Digit `ho` row

main = input `yok` Retry `ha` Perhaps `ha` parse
